setlocal expandtab "replace tabs with spaces
setlocal tabstop=2 "number of spaces to use
setlocal shiftwidth=2 "number of spaces used for indentation
setlocal softtabstop=2 " backspace treats 4 spaces as a tab
setlocal autoindent
setlocal nosmartindent

setlocal formatprg=jq
